class FoodPlansController < ApplicationController
    def create
        # food_plan=FoodPlan.new
        # plan=Plan.find params[:id]
        # carts=current_user.shopping_carts
        # food_plan.plan=plan
        # carts.each do |c| 
        #     food_plan.food=c.food 
        # end
    end
end
